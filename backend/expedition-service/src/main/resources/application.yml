spring:
  application:
    name: expedition-service

---
spring:
  config:
    activate:
      on-profile: docker
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
