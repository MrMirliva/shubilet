spring:
    application:
        name: security-service

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true

---
spring:
    config:
        activate:
            on-profile: docker

    datasource:
        url: jdbc:postgresql://postgres:5432/mainDB
        username: postgres
        password: 123
    jpa:
        hibernate:
            ddl-auto: update